# code-change-plan 输出示例

## 调用方式

~~~text
/code-change-plan

需求：实现用户登录功能
- 支持用户名/密码登录
- 登录成功后返回 7 天有效的 JWT
~~~

## 输出文件

`.code/change-plans/新增用户登录功能-20260824-143052.md`

~~~markdown
# 代码变更方案：新增用户登录功能

**状态：** WAITING_FOR_APPROVAL　**风险：** 中

## 方案结论

**建议：** 附条件批准。身份令牌的返回方式确认后即可实施。

**完成后：** 用户可以使用用户名和密码登录，并获得 7 天有效的身份令牌。

**主要影响：** 指定接口将要求用户携带有效的身份令牌。

**最高风险：** 认证中间件如果应用范围错误，可能阻止公开接口访问。

**需要决定：**

- 身份令牌由浏览器安全 Cookie 自动携带，还是由接口响应正文返回。

## 关键变化

| 功能点 | 当前情况 | 修改后 | 主要影响 |
|---|---|---|---|
| 登录 | 没有认证入口 | 登录接口验证凭据并返回身份令牌 | 新增路由、认证服务和配置 |
| 访问控制 | 指定接口无身份校验 | 只有有效身份令牌才能访问 | 只影响明确列出的受保护路由 |
| 兼容性 | 公开接口匿名访问 | 继续匿名访问 | 现有响应格式和用户查询行为不变 |

---

## AI 执行清单

### 实施阶段

#### 阶段一：准备认证配置

**目标：** 提供签发和校验 JWT 所需的配置。JWT 是用于证明登录身份的令牌。

- `package.json:dependencies`
  - **改动：** 添加项目缺失的 JWT 依赖。
  - **保持：** 不升级无关依赖。
- `.env.example`
  - **改动：** 说明 JWT 密钥和有效期。
  - **保持：** 不写入真实密钥。

**完成标志：** 项目可以读取认证配置并正常构建。

#### 阶段二：实现登录和身份校验

**目标：** 完成凭据验证、JWT 签发和身份识别。

- `src/services/auth.service.ts:AuthService`
  - **改动：** 验证凭据并签发 JWT。
  - **保持：** 复用现有密码哈希，不处理 HTTP 响应。
- `src/middlewares/auth.middleware.ts`
  - **改动：** 校验 JWT 并提供身份上下文。
  - **保持：** 不查询或修改业务数据。

**完成标志：** 服务可以区分有效、无效和过期 JWT。

#### 阶段三：接入路由并补充测试

**目标：** 提供登录入口，并只保护指定接口。

- `src/routes/auth.routes.ts:login`
  - **改动：** 新增登录路由。
  - **保持：** 沿用现有错误格式。
- `src/routes/index.ts:router`
  - **改动：** 注册登录路由。
  - **保持：** 不调整现有路由路径。
- `src/app.ts:createApp`
  - **改动：** 将认证中间件应用到指定接口。
  - **保持：** 公开路由继续允许匿名访问。
- `tests/auth.test.ts`
  - **改动：** 覆盖登录、访问控制和公开路由。
  - **保持：** 不依赖真实外部服务。

**完成标志：** 登录入口和受保护接口通过定向测试。

### 验证

#### 验证阶段一：验证登录行为

- **前置条件：** 阶段二已经完成凭据验证和 JWT 签发。
- **动作：** 分别使用正确密码、错误密码和不存在的用户执行登录测试。
- **预期结果：** 正确凭据返回 JWT；另外两种情况返回相同的 401 响应。

**继续条件：** JWT 内容和错误响应都符合合同后，才能验证访问控制。

**失败处理：** 停止验证，并返回阶段二检查凭据验证或 Token 签发。

#### 验证阶段二：验证访问控制

**承接上一阶段：** 使用阶段一签发并确认有效的 JWT。

- **前置条件：** 认证中间件已经接入指定接口。
- **动作：** 分别使用有效、无效和过期 JWT 访问受保护接口。
- **预期结果：** 只有有效 JWT 可以访问，其余请求返回 401。

**继续条件：** 受保护接口行为正确后，才能执行兼容性回归。

**失败处理：** 停止验证，并返回阶段三检查中间件应用范围。

#### 验证阶段三：验证公开接口兼容性

**承接上一阶段：** 阶段二已经确认受保护接口能够正确识别 JWT。

- **前置条件：** 公开路由未配置认证中间件。
- **动作：** 不携带 JWT 执行现有公开路由测试。
- **预期结果：** 公开路由继续允许匿名访问，响应格式保持不变。

**失败处理：** 停止交付，并返回阶段三缩小认证中间件的应用范围。

### 停止条件

- 实现需要修改用户模型、数据库结构或其他白名单外文件。
- 项目实际密码算法与方案假设不一致。
- Token 传输方式尚未确认。
~~~

## 控制台输出

~~~text
方案：新增用户名密码登录并用 JWT 保护指定接口
风险：中
待决策：Token 传输方式
状态：WAITING_FOR_APPROVAL
文件：.code/change-plans/新增用户登录功能-20260824-143052.md
~~~
