# code-merge-helper 输出示例

## 调用方式

~~~text
/code-merge-helper

分析当前 merge 冲突，生成报告，不修改代码或 Git 状态。
~~~

**场景：** 将 `feature/login` 合并到 `main`。

## 输出文件

`.code/merge-review/合并feature-login到main-20260824-150230.md`

~~~markdown
# Git 冲突解决方案

**状态：** WAITING_FOR_APPROVAL
**操作：** feature/login@e4f5g6h → main@a1b2c3d　**冲突：** 3　**风险：** 高

## 合并结论

**建议：** 附条件继续合并。生产 Node.js 版本确认前不要处理依赖文件。

**合并后：** 用户查询同时保留缓存和密码过滤，登录路由采用合入侧实现。

**主要影响：** 旧缓存需要再次过滤，避免返回密码字段。

**最高风险：** 缓存依赖版本可能与生产 Node.js 不兼容。

**需要决定：**

- 确认生产环境 Node.js 版本，以选择兼容的 Redis 版本。

## 关键冲突

| 功能/文件 | 当前侧变化 | 合入侧变化 | 合并结果 | 风险或决策 |
|---|---|---|---|---|
| 用户查询 | 增加查询缓存 | 过滤密码字段 | 缓存过滤后的对象 | 旧缓存可能含有密码 |
| 登录路由 | 保持原有路由 | 增加登录入口 | 保留登录入口和原有顺序 | 低风险 |
| 依赖 | 增加 Redis | 增加认证依赖 | 合并双方依赖 | 需要确认 Node.js 版本 |

---

## AI 冲突执行信息

### Git 依据

- **当前侧：** main（stage-2：`a1b2c3d`）
- **合入侧：** feature/login（stage-3：`e4f5g6h`）
- **共同基线：** `91ab234`
- `a1b2c3d` 引入 Redis 缓存；`e4f5g6h` 引入字段过滤和认证依赖。

### 解决阶段

#### 阶段一：确认并合并依赖

**目标：** 选择与生产 Node.js 兼容的 Redis 版本。

- `package.json:dependencies`
  - **解决动作：** 确认 Node.js 版本后合并双方依赖。
  - **必须保留：** 缓存和认证功能所需的依赖都存在。

**完成标志：** 依赖安装、依赖树检查和构建全部通过。

#### 阶段二：融合用户查询行为

**目标：** 同时保留缓存和密码过滤。

- `src/services/user.service.ts:findById`
  - **解决动作：** 过滤密码后写入缓存，读取旧缓存时再次过滤。
  - **必须保留：** 缓存命中有效，所有返回路径都不包含密码。

**完成标志：** 缓存命中、未命中和旧缓存都返回安全对象。

#### 阶段三：保留登录路由

**目标：** 接入登录入口，同时维持原有路由行为。

- `src/routes/api.ts:router`
  - **解决动作：** 保留登录路由，并维持原有路由顺序。
  - **必须保留：** 现有路由路径和匹配行为不变。

**完成标志：** 新旧路由测试全部通过。

### 合并验证

#### 验证阶段一：验证双方原有行为

- **前置条件：** 三个解决阶段已经完成，工作区尚未暂存。
- **动作：** 执行缓存、密码过滤和登录路由的定向测试。
- **预期结果：** 当前侧的缓存行为和合入侧的安全行为都通过。

**继续条件：** 双方原有行为全部通过后，才能验证组合行为。

**失败处理：** 停止验证，并返回对应的解决阶段检查遗漏行为。

#### 验证阶段二：验证组合行为

**承接上一阶段：** 阶段一已经确认双方原有行为仍然成立。

- **前置条件：** 缓存中预置一个包含密码字段的旧对象。
- **动作：** 执行用户查询，并再次读取更新后的缓存。
- **预期结果：** 查询结果和更新后的缓存都不包含密码字段。

**继续条件：** 组合行为通过后，才能检查 Git 完整性。

**失败处理：** 停止验证，并返回解决阶段二检查过滤顺序。

#### 验证阶段三：检查 Git 完整性

**承接上一阶段：** 阶段二已经确认融合后的业务行为正确。

- **前置条件：** 所有报告内冲突已经按方案解决。
- **动作：** 执行 `git diff --check`，并检查冲突标记和未合并索引。
- **预期结果：** 没有格式错误、冲突标记或未合并文件。

**失败处理：** 不暂存、不提交，返回对应解决阶段处理遗留问题。

### 停止条件

- 生产 Node.js 版本尚未确认。
- stage 来源变化或出现报告之外的新冲突。
- 旧缓存兼容无法在不扩大范围的情况下解决。
- 任一单侧或组合行为验证失败。

### 恢复方式

当前仍在 merge 冲突处理中且尚未提交，可使用 `git merge --abort`。
~~~

## 控制台输出

~~~text
合并：同时保留缓存、密码过滤和登录路由；依赖版本待确认
冲突：3
风险：高
待决策：生产 Node.js 版本
状态：WAITING_FOR_APPROVAL
文件：.code/merge-review/合并feature-login到main-20260824-150230.md
~~~
