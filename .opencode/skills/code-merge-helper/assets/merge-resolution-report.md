# Git 合并冲突分析报告

**Plan ID:** `<plan-id>`  
**创建时间:** `<timestamp>`  
**操作:** `<source-branch>` → `<target-branch>`  
**冲突文件数:** `<count>`  
**高风险冲突:** `<count>`  
**状态:** 等待审批

---

## 1️⃣ 冲突概览

| 文件 | 类型 | 风险 | 推荐方案 |
|------|------|------|---------|
| `<file-1>` | 逻辑冲突 | ⚠️ 中 | 语义融合 |
| `<file-2>` | 文本冲突 | ✅ 低 | 保留 stage-2 |
| **`<file-3>`** | **依赖冲突** | **🔴 高** | **需人工决策** |

**说明：**
- **stage-2:** 当前分支（`<target-branch>`）
- **stage-3:** 合入分支（`<source-branch>`）
- **common-base:** 共同祖先（`<base-sha>`）

---

## 2️⃣ 冲突分析

### 🔸 C-01: `<file-path>`（<风险等级>）

**发生了什么？**
- **stage-2 (<target-branch>):** <作者> 在 <日期> 做了 <改动描述>（提交 `<sha>`）→ 目的：<为什么改>
- **stage-3 (<source-branch>):** <作者> 在 <日期> 做了 <改动描述>（提交 `<sha>`）→ 目的：<为什么改>
- **common-base:** <基线状态描述>

**推荐方案：<方案类型>**
```<language>
<推荐的合并后代码>
```

**理由：** <为什么选择这个方案>

---

### 🔸 C-02: `<file-path>`（<风险等级>）

**发生了什么？**
- **stage-2:** <改动描述>
- **stage-3:** <改动描述>

**推荐方案：<方案类型>**
<方案描述或代码>

**理由：** <为什么选择这个方案>

---

### 🔸 C-03: `<file-path>`（高风险）⚠️

**发生了什么？**
- **stage-2:** <改动描述>
- **stage-3:** <改动描述>

**❓ 需要人工决策：**
> <具体的决策问题>
> - 选项 A: <选项描述> → <优缺点>
> - 选项 B: <选项描述> → <优缺点>

**推荐方案：** <推荐选项> - <理由>

---

## 3️⃣ 操作步骤

```bash
# 1. 确认 Git 状态
git status --short

# 2. 解决冲突（按上述方案编辑文件）
# - 编辑 <file-1>（按 C-01 方案）
# - 编辑 <file-2>（按 C-02 方案）
# - 编辑 <file-3>（按 C-03 方案，确认人工决策后）

# 3. 验证编译
<编译命令>

# 4. 运行测试
<测试命令>

# 5. 检查冲突标记已清除
git diff --check

# 6. 完成合并
git add .
git commit -m "<提交信息>"
```

---

## 4️⃣ 验证清单

- [ ] 所有冲突文件已按方案编辑
- [ ] 冲突标记（`<<<<<<<`, `=======`, `>>>>>>>`）已全部清除
- [ ] 编译无错误
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] stage-2 的功能正常运行
- [ ] stage-3 的功能正常运行
- [ ] 手动验证关键功能

---

## 5️⃣ 风险与回滚

### 风险
- 🔴 **高风险：** <风险描述> → 缓解：<缓解措施>
- ⚠️ **中风险：** <风险描述> → 缓解：<缓解措施>
- ✅ **低风险：** <风险描述> → 缓解：<缓解措施>

### 回滚方案
```bash
# 如果还在合并过程中
git merge --abort

# 如果已经 commit
git revert <commit-sha>

# 如果已经 push
git revert <commit-sha>
git push
```

---

## ✅ 审批

**状态:** WAITING_FOR_APPROVAL

**待决策问题：**
- [ ] <需要确认的问题 1>
- [ ] <需要确认的问题 2>

**审批通过后执行：**
按照第 3 章"操作步骤"执行合并。

---

**报告结束**
