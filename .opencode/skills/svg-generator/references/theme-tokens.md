# 主题与颜色令牌

主题名只帮助用户识别风格，不代表业务领域。参考图只用于提取颜色，不复制其业务内容、图标或布局。

| 主题 | DSL 键 | Primary | Secondary | Tertiary | Accent |
| --- | --- | --- | --- | --- | --- |
| 科技蓝（默认） | `tech-blue` | `#2E69E1` | `#1A388D` | `#819FDE` | `#1A388D` |
| 活力多彩 | `vibrant` | `#1B9CDE` | `#A039B9` | `#39A95B` | `#FE9022` |
| 稳健聚焦 | `focused` | `#1F5CD7` | `#12939D` | `#4B9654` | `#FF6B1A` |

## 选择规则

优先级：用户逐槽颜色 > 明确主题 > 可明确映射的视觉描述 > 一次询问结果 > 默认主题。

1. 已提供主题、合法颜色、品牌色或明确视觉描述时，直接使用。
2. 完全没有颜色信号时只询问一次：请选择科技蓝（默认）、活力多彩、稳健聚焦，或提供自定义颜色。
3. 用户回复“默认、随便、你决定、都可以、直接生成”或仍未选择时，使用 `tech-blue/light`，不再追问。
4. 用户要求不提问或环境不支持交互时，直接使用默认主题并说明。
5. 不根据节点名称或业务领域擅自推断主题。

## 色槽角色

- `primary`：主结构、主要入口和主路径。
- `secondary`：辅助结构和第二类关系。
- `tertiary`：并列结构和补充区域。
- `accent`：唯一焦点、选中项或关键结论，不大面积铺满。
- `success/warning/error/async`：只表达真实状态或关系。

运行 `scripts/palette.py` 生成 `base/strong/soft/border/foreground`。保留用户提供的合法色槽，其余由所选主题确定性补齐；极浅或极深输入也必须生成可见边框和填充。浅色与深色只切换令牌，不改变几何结构。
