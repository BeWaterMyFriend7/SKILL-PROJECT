# 主题与颜色令牌

主题名必须严格使用用户指定的文字，不得改写、翻译或替换为其他名称。参考图不用于复制业务内容，但必须提炼其色彩层级、卡片体系、标题关系、轻量质感和结构表达。

| 主题名称（严格按此显示） | DSL 键 | 关键配色 |
| --- | --- | --- |
| Tech Blue｜科技蓝（默认） | `tech-blue` | 背景 `#F4F7FC`；容器 `#E9EFF8`；卡片 `#FDFEFF`；主蓝 `#3B6EDC`；深蓝 `#2D56B3`；中蓝 `#5B8FF9`；数据青 `#2AA7C8`；边框 `#C9D7EA` |
| Vibrant ｜活力 | `vibrant` | 蓝 `#3B82F6`；紫 `#8B5CF6`；青 `#06B6D4`；绿 `#22C55E`；橙 `#F59E0B`；红 `#EF4444`；背景 `#FAFBFC`；边框 `#D1D5DB` |
| Mint green｜清爽绿 | `mint-green` | 背景 `#F8FAFC`；容器 `#EEF6F4`；卡片 `#FFFFFF`；薄荷绿 `#2E8B57`；浅绿 `#DDF5E8`；青绿 `#14B8A6`；浅青 `#CCFBF1`；边框 `#C7D2D9` |
| Steady Red & Blue｜稳重红蓝 | `steady-red-blue` | 背景 `#F6F7FB`；容器 `#E9EEF5`；卡片 `#FFFFFF`；红 `#C62828`；深海军蓝 `#243B63`；浅红 `#FBE4E4`；强调蓝 `#4A90E2`；边框 `#CAD3DF` |

## 选择规则

优先级：用户逐槽颜色 > 明确主题 > 可明确映射的视觉描述 > 一次询问结果 > 默认主题。

1. 已提供主题、合法颜色、品牌色或明确视觉描述时，直接使用。
2. 完全没有颜色信号时只询问一次：请选择 Tech Blue｜科技蓝（默认）、Vibrant ｜活力、Mint green｜清爽绿、Steady Red & Blue｜稳重红蓝，或提供自定义颜色。
3. 用户回复“默认、随便、你决定、都可以、直接生成”或仍未选择时，使用 `tech-blue/light`，不再追问。
4. 用户要求不提问或环境不支持交互时，直接使用默认主题并说明。
5. 不根据节点名称或业务领域擅自推断主题。

## 色板与结构角色

主题只定义颜色家族，不把具体颜色永久绑定到横向或纵向组件。生成前先判断布局和主叙事轴，再运行 `scripts/style_map.py` 将色板映射到结构角色。

- `axis-main`：主叙事轴和核心阅读路径。
- `axis-cross`：交叉分层、阶段或分组。
- `side-rail`：侧栏、治理域、边界或参与方。
- `focus`：唯一焦点、选中项或关键结论。
- `data-flow`：数据、反馈和观测链路。
- `group/connector`：普通并列结构和非状态主连接。

背景、卡片、边框、文字和状态色保持固定语义，不参与轴向交换。同类组件必须使用相同角色，禁止按单个节点随机换色。

## 兼容色槽

- `primary`：主结构、主要入口和主路径。
- `secondary`：辅助结构和第二类关系。
- `tertiary`：并列结构和补充区域。
- `accent`：唯一焦点、选中项或关键结论，不大面积铺满。
- `success/warning/error/async`：只表达真实状态或关系。

运行 `scripts/palette.py` 生成基础色板，再由 `scripts/style_map.py` 生成各结构角色的 `base/strong/soft/border/foreground`。保留用户提供的合法色槽或角色覆盖，其余确定性补齐；浅色与深色只切换令牌，不改变几何结构。
