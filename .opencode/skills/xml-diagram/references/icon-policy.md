# 图标策略

- AI 根据内容语义决定是否使用图标，不为每个卡片机械添加。
- 普通图通常使用 0～4 个，这是克制建议，不是限额；不设置硬上限。
- 超过常见数量时，每个图标都必须有明确的导航、识别或焦点价值，并通过密度、重叠和缩小可读性检查。
- 同一张图只使用一种图标风格、统一线宽和视觉尺寸；图标不得抢过标题与结构。
- 优先使用 Draw.io 原生形状。确有必要时使用清理后的内嵌单色 SVG，颜色跟随主题令牌。
- 禁止外部 URL、Emoji、图标字体、未内嵌本地路径、位图图标和不可编辑的大型插画。
- 没有图标不会降低信息理解时，保持无图标。
